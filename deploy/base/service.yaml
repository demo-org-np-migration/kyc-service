apiVersion: v1
kind: Service
metadata:
  name: kyc-service
  labels:
    app.kubernetes.io/name: kyc-service
    app.kubernetes.io/part-of: cauri
    cauri.io/team: risk
spec:
  selector:
    app.kubernetes.io/name: kyc-service
  ports:
    - name: http
      port: 8080
      targetPort: http
